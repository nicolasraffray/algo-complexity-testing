

describe('Timing', () => {
  
  describe('iterat', () => {
    it('takes the iteration start time and end time and pushes it to time array', () => {
      let Time = new Timing(sort)
      
      
    })
  })
})